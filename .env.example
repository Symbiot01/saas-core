# Firebase Admin SDK Configuration
# You must set ONE of the following three options:

# Option 1: Service Account JSON file path (recommended for local development)
SAAS_CORE_FIREBASE_CREDENTIALS_PATH=/path/to/serviceAccountKey.json

# Option 2: Service Account JSON as string (recommended for containers/CI/CD)
# Pass the entire JSON content as a string (can be minified or formatted)
# SAAS_CORE_FIREBASE_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'

# Option 3: Project ID (uses Application Default Credentials)
# Use this if you're running on Google Cloud (GCE, Cloud Run, etc.)
# SAAS_CORE_GOOGLE_PROJECT_ID=your-project-id

# Email Verification
# Set to false to allow unverified emails (not recommended for production)
SAAS_CORE_REQUIRE_EMAIL_VERIFIED=true

# Database (placeholder for future)
# SAAS_CORE_DATABASE_URL=postgresql://user:pass@localhost/dbname
